#include <bits/stdc++.h>
using namespace std;
int main(){

	int a, b, c;
	cin >> a >> b >> c;
	if (a == 0 && b == 0 && c == 0) {
		cout << '0';
	}
	if (a != 0) cout << a;
	if (b != 0){
		
	}
	return 0;
}