#include <iostream> 
#include <cmath>
using namespace std; 
int main() {


int n, k, z, m;
cin >> n;
n = k*m;


if (k==1&& m==n && n!=sqrt(n)*sqrt(n) || k==n&&m==n&&n!=sqrt(n)*sqrt(n)){
	m =1;
	} if (m=1)  {
		cout << "YES";
}else 
cout << "NO";

return 0;
}
