#include <bits/stdc++.h>
using namespace std;
int main(){

	int n;
	cin >> n;
	string s, longest, shortest;
	int cnt;
	map<string, int> m;

	while(cin >> s){
		m[s]++;
		if(s.size() > longest.size()){
			l = s;
		}
		if(s.size() < shortest.s)
	}

map<string, int> :: iterator it;

		for(it = m.begin(); it != m.end(); it++){
			cout << (*it).first << " " << (*it).second << endl;
		}

return 0;
}