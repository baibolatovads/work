#include <bits/stdc++.h>
using namespace std;

int main(){

	string s;
	cin >> s;
	vector<char> v;
	v.push_back(s[0]);
	set<char> s;

	for(int i = 0; i < s.size(); i++){
		if(v.contai)
		v.push_back(s[i]);
	}

	for(int i = 0; i < v.size(); i++){
		if(v[i])
	}

	return 0;
}